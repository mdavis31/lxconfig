# Commands (Bash)

**KDE Settings Saver:**
    
    $ python -m konsave 
    -l                  #list profiles
    -s <name>           #save profile
    -s <name> -f        #save (overwrite)
    -a <name>           #apply profile
    -i, -e              #import, export (.knsv)

